<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<!-- The bundle stays "Bookmark AI.app" (the path Safari's registration and
	     /Applications already use); the display name tells this companion apart
	     from the native desktop app (apps/macos) in Launchpad and the App Store. -->
	<key>CFBundleName</key>
	<string>Bookmark AI</string>
	<key>CFBundleDisplayName</key>
	<string>Bookmark AI for Safari</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>LSMinimumSystemVersion</key>
	<string>$(MACOSX_DEPLOYMENT_TARGET)</string>
	<key>LSApplicationCategoryType</key>
	<string>public.app-category.productivity</string>
	<!-- Export compliance: the app makes no network calls of its own and the
	     extension uses only HTTPS through WebKit — exempt. Saying so here skips
	     the encryption questionnaire on every App Store Connect upload. -->
	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>
	<!-- Menu-bar companion: no Dock icon. The setup window still opens on first
	     launch and whenever the app is opened again (Finder / Launchpad / `open`). -->
	<key>LSUIElement</key>
	<true/>
	<key>NSHumanReadableCopyright</key>
	<string>© 2026 Tarachand Gupta</string>
	<key>NSPrincipalClass</key>
	<string>NSApplication</string>
</dict>
</plist>
