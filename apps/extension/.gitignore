node_modules/
.output/
.wxt/
.turbo/
.env*
!.env.example
# Committed per-target build defaults (all PUBLIC values — publishable keys +
# URLs). A local `.env` / `.env.*.local` stays ignored for private overrides.
!.env.development
!.env.production
!.env.dev-remote
# CRX private key — its public half is committed in wxt.config.ts
.keys/
# Safari wrapper: safari-app/project.yml is the source of truth; everything the
# pipeline generates is ignored (also listed in the root .gitignore).
safari-app/*.xcodeproj/
safari-app/Extension/Resources/
safari-app/DerivedData/
safari-app/build/
# Stray safari-web-extension-converter output from the retired flow.
Bookmark AI/
safari-xcode/
