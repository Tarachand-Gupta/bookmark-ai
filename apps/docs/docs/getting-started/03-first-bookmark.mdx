---
slug: /getting-started/first-bookmark
title: Save your first bookmark
description: Save a page in one click and watch it get categorized, tagged, and embedded.
sidebar_custom_props:
  icon: 🔖
---

import Screenshot from '@site/src/components/Screenshot';

# Save your first bookmark

1. Open a page you want to keep.
2. Click the Bookmark AI extension (or press **Alt+Shift+S**).
3. Click **Save**.

<Screenshot
  alt="The signed-in extension popup showing the current tab (Bookmark - Wikipedia) with a large 'Save bookmark' button, session buttons below, and the live-session card."
  dark="/img/screenshots/popup-main.png"
  maxWidth={360}
  caption="The popup detects the active tab — one click on Save bookmark files it."
/>

That's it. Behind the scenes the app:

- reads the page and scrapes its **Open Graph** data (title, description, image),
- sorts it into a **category** and assigns **tags** — no folders to maintain,
- builds a **semantic embedding** so the page is findable by meaning.

The save returns immediately — the heavier AI work runs right after, so the
page is searchable moments later.

:::note
Bookmarks are keyed by URL. Saving the same page again updates it rather than
creating a duplicate, and re-runs categorization and embedding.
:::

Open [bookmark-ai.cloud](https://bookmark-ai.cloud) and your new bookmark is in
the library, filed under its category with its tags. From here you can
[search](/guides/search) or [ask AI](/guides/ask-ai) about it.

<Screenshot
  alt="The library grid after saving, showing the new bookmark as a card filed under its category with tags."
  dark="/img/screenshots/library-dark.jpg"
  light="/img/screenshots/library-light.jpg"
  caption="Every save lands in the library, filed and tagged automatically."
/>

## Next

Save a whole window of tabs at once →
[Save a session](/getting-started/first-session).
