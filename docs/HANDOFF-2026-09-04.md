# Session handoff — 2026-09-03/04 build cycle

Untracked on purpose (session notes never get committed). Commits `fc9540f…657b9dc` (6) on top of the
macOS baseline `7a9efce`.

## 1. Shipped in this cycle (all QA'd locally with screenshots; prod verification pending the deploy)

**API / packages** (`fc9540f`, `116aa42`)
- Tenant migration **v14 `skills-and-ai-mode`** (skills table, `user_settings.ai_mode`), master **v4 `tenant-plan`**
  (`tenants.plan DEFAULT 'free'`; v3 was burned by the reverted newtab feature). Export `SCHEMA_VERSION` 5.
- Settings `aiMode: included|own` — a mode switch never touches the key; `apiKey:""` is the only remove;
  provider change keeps the model; `ownKeyReady`; Google keys default to `gemini-2.5-flash`.
- `/api/chat`: `{message, conversationId, timezone}` after turn 1 (history loads server-side); `file` parts
  validated (415/413/400) and text docs normalised; `sendReasoning` + Gemini `includeThoughts`;
  `X-Ai-Source: included|own|own-fallback` (own key takes over when weekly credits are exhausted);
  `X-Ai-Note: own-key-incomplete`; pre-stream awaits parallelised; message ids scoped per conversation;
  empty turns stream a readable `error` chunk and are never persisted; provider errors are readable and
  secret-redacted; attachment media type resolved from magic bytes.
- Skills: `/api/skills` CRUD, `/api/skills/import` (SKILL.md), tools `useSkill` / `createSkill` /
  `installSkill({url})`, SKILLS index in the prompt. Docs: `docs/features/skills.md`.
- Prompt rewritten around the primitives (bookmarks, sessions, live tabs, Ask AI, skills, MCP, settings)
  with exploration playbooks; answers use product language, link titles, never claim it can't remember.
- `/api/account` → `plan: "free"` for everyone. `PLAN_FEATURES` in `packages/types/src/plan.ts`.

**Web** (`2904643`): homepage Pricing section (one card: Free $0 + four features | Pro "Coming soon",
`#pricing` nav); Settings → AI mode card; Settings → Account plan badge; Settings → Skills + `⋯` menu in
Ask AI (SKILL.md import via file/drag-drop); Ask AI reasoning disclosure, tool rows for all states,
instant Thinking placeholder, attachments (allowlist, 1568 px downscale, pills, lightbox); pointer cursor
on every button.

**macOS** (`657b9dc`): `⋯` menu (Skills sheet + Import…), Settings → MCP token management, AI mode
switch, Account badge, chat reasoning/tool rows/attachments, **history popover** (search, day groups,
hover `⋯` → Delete… with confirmation) replacing the old menu. 97 tests, 27 render previews.

**Mobile** (`da86736`): attachments (Photos/Files, allowlist, downscale, pills, lightbox,
attachment-only sends), reasoning + tool rows, Thinking placeholder, plan card, AI mode switch when a
key exists. iOS verified on simulator; Android debug build verified incl. the photo picker. 50 tests.
**Release-config build against prod NOT done** (memory: release-config-qa-gap).

**Extension** (`e4afae3`): Firefox sign-in fixed (Clerk `host_permissions` shim), every fetch bounded,
clerk-js dropped from the popup (1.14 MB → 236 KB), prod builds no longer POST diagnostics to
localhost, `build:firefox:local` / `build:safari:local`, native-sync import gate, popup polish. 140 tests.
Zips in `apps/extension/.output/` (chrome/firefox/safari 0.1.1). Safari wrapper installed at
`/Applications/Bookmark AI.app` (only copy) and the extension shows ENABLED in Safari settings.
Firefox: no AMO credentials → temporary add-on only (`about:debugging` → Load Temporary Add-on →
`.output/firefox-mv2/manifest.json`).

## 2. Release status (2026-09-07)
1. Tara ran `vercel deploy --prod --yes` (the agent is blocked from it by the permission classifier).
   New build confirmed live: pricing HTML, `X-Ai-Source`/`X-Ai-Note` CORS headers, health OK.
2. Signed-out prod smoke (headless): 20/20 PASS — pricing light/dark/390, clean 401s on every new
   route, sign-in/privacy/terms/manifest, health median ~400 ms.
3. Signed-in prod walk on Tara's account (Claude Chrome extension): Settings → AI mode switch with live
   credits, Account plan card, Skills manager (empty state + Import/templates), Ask AI "What am I working
   on?" → Thinking placeholder → "Thought for 1 s" → live-tabs card (68 tabs on 2 devices) → answer;
   follow-up kept context; image attachment re-encoded to JPEG (56 KB), thumbnail + correct description.
   Test conversation deleted. Cost: ~52 free credits. Finding: the model wrote the reply after live tabs
   only and promised "Next, I will look into your saved sessions…" — prompt fix queued (backend).
4. Commits after the first deploy (need a second `vercel deploy --prod`): `aaa362a` prompt playbook
   fix, `e625a50` releases API (master v5 `app_releases`, public GET, admin PUT/DELETE), `dca2358`
   macOS auth gate + identity, `8c67c8b` web identity row + admin Releases section, `c84acad` route
   export fix (Next route-module type check), `0953b0b` mobile update banner (Home, 70 tests),
   `bbdf6ab` macOS update banner (sidebar, above the footer, 136 tests). Both banners poll the
   public `GET /api/app/releases` every 6 h, snooze 24 h per version, and go blocking when
   `minSupportedVersion` is above the running build (CONTRACT §12). After the deploy, publish the
   current versions in Settings → Releases (macOS 0.1.0 build 1, iOS/Android 1.0.0 build 1) so the
   comparison has a baseline; an empty table means no banner anywhere.
5. Still open: `git push` (Tara decides), Chrome Web Store upload (never run), Firefox AMO signing
   (no credentials), mobile Release builds, Developer ID + notarization + Sparkle for Mac auto-update
   (discussed, not built).

## 3. Known gaps / follow-ups
- Daily abuse caps still apply (200 saves, 50 sessions, 100 chats, 500 searches/day under
  `MULTI_TENANT=1`; live server 2,000 pushes/device/day) — the pricing card says "Fair-use limits apply".
- `own-fallback` and `own-key-incomplete` paths are unit-tested and API-verified but never observed
  with a real provider key. `installSkill` was live-verified from a raw GitHub URL only on the API.
- Export/import: `insertMessage` still uses `INSERT OR REPLACE` on bundle-supplied ids (idempotent
  re-import by design; a crafted bundle could collide across conversations).
- Gemini sometimes writes a one-line aside between tool calls (prompt asks it not to; clients render
  text parts in stream order so nothing is lost).
- Mobile: no skills management UI (server-side skills still apply); swipe-to-delete not added
  (long-press → confirm).
- Langfuse/observability untouched, as requested.

## 4. Process notes
- QA fixtures on the shared local tenant collided a few times (fake BYOK keys poisoning other agents'
  chats for seconds). Keep settings-mutation windows short; prefix fixtures; delete by id.
- `pkill -x BookmarkAI` kills the iOS simulator app of the same name — never do it.
- A stale `next-server` from Aug 30 on :3000 served 500s — kill via `lsof -nP -iTCP:3000 -sTCP:LISTEN`.

## 5. Store submissions + mobile store readiness (2026-09-07, later)
**Extension 0.1.2** (`964e17a`, `f791599`): production builds carry ONLY the four prod hosts
(apex, www, clerk., live.); Firefox manifest declares truthful
`data_collection_permissions` (`authenticationInfo`, `bookmarksInfo`, `browsingActivity`) and
`strict_min_version 140.0`. `pnpm --filter @bookmark-ai/extension release:zip` → `.output/`:
`*-chrome.zip` (CI updates), `*-chrome-store.zip` (first CWS upload only: manifest `key` stripped,
`key.pem` at the zip root so the pinned id `ffhbgpgebpmofjkehpjcemepbgcmoelp` survives), `*-firefox.zip`,
`*-sources.zip` (AMO source with BUILD.md; reproduced byte-identical). Paste sheets:
`apps/extension/STORE-LISTING.md` (CWS) and `AMO-LISTING.md`; assets in `apps/extension/store-assets/`.
- **Firefox**: submission form fully filled and PARKED unsubmitted in the Claude Chrome tab
  "Describe Add-on :: Bookmark AI". Tara replaces the `TEST ACCOUNT:` line in Notes to Reviewer and
  clicks Submit Version; then attach `bookmark-aiextension-0.1.2-sources.zip` on the version's
  Source Code field. The CWS dashboard cannot be scripted (Tara clicks along the paste sheet).
- After approval: replace the example.com placeholders in `apps/web/lib/extension-links.ts`
  (Chrome `https://chromewebstore.google.com/detail/ffhbgpgebpmofjkehpjcemepbgcmoelp`, Firefox
  `https://addons.mozilla.org/firefox/addon/bookmark-ai/`) and set the `CWS_*` GitHub secrets.

**Mobile** (`10549f2`, `e01a015`, `c4ecfe1`, `2241ddb`): Settings → Legal & Support rows, Sign Out /
Delete Account, version row; Face ID purpose string, truthful `PrivacyInfo`, `ITSAppUsesNonExemptEncryption
false`; consent line on sign-in; iPad sign-in column. Full audit + paste-ready store metadata in
`docs/mobile-store-readiness.md` (untracked). Release-config builds against prod verified signed-out;
Opus signed-in walkthrough found 4 layout/appearance defects → Fable builder fixing (`layout.ts`,
`appearance.ts`, `useContentWidth`). **Web** (`52a797b`): privacy page rewritten truthfully (Langfuse,
attachments, device data, `#delete-account` anchor), public `/support` page.

**Decision — Sign in with Apple (guideline 4.8)**: Tara chose Option A: keep Google + email, ADD Apple
everywhere. Web gets the button from the Clerk prebuilt `<SignIn>` once the Apple connection is enabled
in the dashboard; macOS (web sign-in in WKWebView) and the extension (mirrors the web session) inherit
it; mobile needs code: `@clerk/clerk-expo` → `@clerk/expo` + `@clerk/expo/apple` `useSignInWithApple`,
`expo-apple-authentication`, `expo-crypto`, `ios.usesAppleSignIn`, App ID capability, Clerk Native
application (Team `L3PP7DQZWS`, bundle `ai.bookmarkai`), token revocation on account deletion.
Blocked on Tara's Apple Developer enrollment (Services ID + .p8 for the web flow; entitlement needs a
paid team). Prepare the mobile code once the layout builder is done touching `apps/mobile`.

**Tara's queue**: reviewer test account (email+password, seeded) on prod → Firefox Submit → CWS upload
(`-chrome-store.zip`, verify the id in the dashboard URL) → `! vercel deploy --prod --yes` from the repo
root → publish versions in Settings → Releases → Apple Developer + Google Play enrollment.

### 5b. Later on 2026-09-07 (morning IST)
- **Reviewer account on prod**: `test@bookmark-ai.cloud` (password with Tara; never in the repo). Signed in via
  password only (prod Clerk: password first factor, no 2FA, HIBP check on sign-in). Seeded through the prod API
  from a signed-in Chrome for Testing window (scratchpad `seed/`): 40 bookmarks (10 categories, 24 tags, 20
  days, 6 devices), 4 AI-named sessions, 2 skills, 1 chat. Embeddings were 16/40 right after seeding — the
  daily cron (`30 3 * * *` UTC = 09:00 IST) sweeps the rest; verify. Three YouTube saves titled "- YouTube" →
  engine fix `ef45341` (`packages/engine/src/title.ts`, `pickBookmarkTitle`); after the next prod deploy run
  the scratchpad `seed/fix-titles.mjs` (re-saves the 3 URLs) or re-save them by hand.
- **CWS**: first upload failed with "Can not process the key.pem file." — the pinned key is PKCS#1; the
  dashboard wants PKCS#8. `11b21bc` re-wraps it in store-zip.mjs; re-upload the regenerated
  `bookmark-aiextension-0.1.2-chrome-store.zip`.
- **AMO**: Notes to Reviewer now carries the reviewer EMAIL; Tara types the password into the marked slot and
  submits; then attach the sources zip.
- **Mobile** `c1ca62d`: iPad 760pt column + 2/3/4-col grid, live theme in modals (overFullScreen), chat empty
  state, avatar ring; 89 tests. **macOS** `e35d6e5`: identity loads after in-app sign-in (SignInSheet awaited
  /api/me inside a task SwiftUI cancelled on dismiss), footer = email middle-truncated; 144 tests.
- **Sign in with Apple**: dev Clerk instance has Apple enabled (shared credentials) and the iOS app registered
  under Native applications (L3PP7DQZWS / ai.bookmarkai). Web dev sign-in → Apple button →
  appleid.apple.com verified. Production needs: Apple connection with custom credentials (Services ID, Team ID,
  Key ID, .p8) + the same Native applications entry. Builders in flight: mobile native Apple (`@clerk/expo`),
  Safari Mac App Store readiness (`docs/safari-store-readiness.md`).
- **Mobile native Sign in with Apple** `50eda77`: `@clerk/clerk-expo` → `@clerk/expo` 4.6.5 (screens on
  `@clerk/expo/legacy`, `useSignInWithApple` from `@clerk/expo/apple`); `expo-apple-authentication` 57.0.1 +
  `ios.usesAppleSignIn`; the Clerk native module is EXCLUDED from autolinking (would force iOS 17 + pull
  clerk-ios/compose) — JS no-ops without it. System Apple button above Google, 50pt shared height, iOS only.
  Clerk does NOT revoke Apple tokens on user deletion → DeleteAccountSheet tells Apple users the Settings
  path (readiness doc A9 has the full-revocation design). 102 tests. Prod needs: App ID capability, Clerk prod
  Apple connection with custom creds + Private Email Relay source, Native applications entry.
- **Session lifetime (Tara's ask)**: the 7-day logout is Clerk's fixed Hobby-plan maximum lifetime; no code
  limit anywhere; the extension rides 90-day device tokens. Fix = Clerk Pro ($25/mo) → prod Sessions:
  Inactivity timeout 60d ON, Maximum lifetime OFF (or 90d). No client code change.

## 5c · 2026-09-07 morning — store submissions + Safari wrapper + Mac footer

- **AMO**: version 0.1.2 SUBMITTED by Tara. Source-code question answered **Yes** (WXT/Vite bundles,
  minifies, Tailwind generates CSS); `bookmark-aiextension-0.1.2-sources.zip` attached (132 files:
  `apps/extension` 111, `packages/types` 16, 5 root files incl. generated `BUILD.md`; the four committed
  `.env.*` build configs are inside — publishable keys + URLs only). Reviewer notes carry the test-account
  email; Tara typed the password. After approval: fill `EXTENSION_STORE_URLS` in
  `apps/web/lib/extension-links.ts`.
- **CWS**: Tara re-uploading the 07:14 `-chrome-store.zip` (PKCS#8 `key.pem` verified). Listing text +
  justifications in `apps/extension/STORE-LISTING.md`; graphics in `apps/extension/store-assets/` (five
  1280×800 + 440×280 tile). Verify item id `ffhbgpgebpmofjkehpjcemepbgcmoelp` in the dashboard URL.
  Claude-in-Chrome cannot script the CWS dashboard — Tara drives it.
- **Safari Mac App Store wrapper** (`e28565f`, `dee6a75`, `3af2cf0`): committed XcodeGen project at
  `apps/extension/safari-app/` (ids `ai.bookmark.safari` / `.Extension`, team L3PP7DQZWS, sandbox-only,
  privacy manifests, versions from package.json → 0.1.2/10200), pipeline `pnpm --filter
  @bookmark-ai/extension safari:xcode -- [--build|--archive [--unsigned]|--install]`. Companion (menu-bar)
  app shows the REAL sign-in state: Safari-only `nativeMessaging` → `lib/native-auth-report.ts` (deduped,
  never a token) → appex handler → App Group `L3PP7DQZWS.ai.bookmark.safari` (read the group plist path;
  `defaults read <suite>` says "does not exist") → steps 1+2 turn green, "Signed in as ‹email›", Sign In
  hidden. Chrome/Firefox manifests byte-identical to the frozen zips; `background.js` +1.4 KB (module
  early-returns off the inlined BROWSER constant). Extension tests 20 files / 178. NEEDS-TARA (ordered) in
  `docs/safari-store-readiness.md` §6: confirm bundle ids → App IDs → Xcode account → ASC record →
  **deploy prod so `/support` stops 404-ing** → archive/upload → metadata/App Privacy/age rating →
  reviewer password in App Review Information → Safari screenshots (store assets show Chrome) → submit.
- **macOS** `a110efc`: footer first line = account NAME (email in tooltip; Settings ▸ Account shows both).
  Lesson: Tara's "not fixed" report on e35d6e5 was a STALE Debug binary (built 05:06, fix 07:40) — every
  Mac fix now ends with rebuild + `osascript … quit` + relaunch + screenshot.
- **Model rules (Tara)**: small fixes on Opus, Sonnet allowed for testing small fixes, big builds Fable, one
  builder at a time, justify usage.
- Later fixes the same morning: `19bb2b1` companion opens every link in Safari (label "Open Bookmark AI in
  Safari"; no API can open the popup), `f5c519e` Safari popup follows the system appearance
  (`color-scheme: light dark` on `:root` + meta — WebKit popovers render light until the document declares
  dark support), `c8b0216` each save embeds ITS OWN row + 1 straggler (`apps/web/lib/server/post-save-embed.ts`;
  the old `embedPending(…, 5)` in every post-save hook made N burst saves race on the same 5 oldest rows —
  Langfuse showed 64 sweeps × 5 for 16 vectors; MCP `save_bookmark` had the same bug).
- Reviewer library: 40/40 embedded at 09:23 IST — the Vercel cron fires ~09:23 IST daily (03:53 UTC, ~22 min
  after its `30 3` schedule) and DID include the new tenant (10+10+4 in three batches). Langfuse query script:
  scratchpad `seed/langfuse-embed.mjs [hours]` (keys from root `.env`, never printed).
- Still pending: `seed/fix-titles.mjs` after the next prod deploy; Clerk Pro + 60-day inactivity timeout; Apple
  SSO on prod Clerk; close Claude-in-Chrome Clerk tab 610339380.
